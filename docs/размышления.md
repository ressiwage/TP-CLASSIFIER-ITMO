в файле с размышлениями я обычно записываю свои мысли. это для меня, вам читать необзятально.

КУРС ДОЛЛАРА: 80

стоимость ручного труда: 150р за тикет
SLA 15 минут: система должна отвечать за 15 минут

CSAT 4,2/5; Reopen rate 9%: хз вообще как эту дрисню улучшить. раг? но это субъективно.
доля типовых обращений 40%: 40% обращений можно отдать тупому нейроболвану, 60% -- умному или натуральному.

гипотеза 1: если хотим оптимизировать ВСЕ обращения -- нужен тяжелый квен. 
гипотеза 2: для оптимизации типовых обращений хватит легкого квена/сопоставления ковариационных расстояний.
гипотеза 3: не существует прибыльных тяжелых нейронок, значит г1 возможно нецелесообразна.

Model	        Base Input Tokens	5m Cache Writes	1h Cache Writes	Cache Hits & Refreshes	Output Tokens
>>Claude Opus 5	$5 / MTok	        $6.25 / MTok	$10 / MTok	    $0.50 / MTok	        $25 / MTok
Claude Opus 4.8	$5 / MTok	        $6.25 / MTok	$10 / MTok	    $0.50 / MTok	        $25 / MTok
Claude Opus 4.7	$5 / MTok	        $6.25 / MTok	$10 / MTok	    $0.50 / MTok	        $25 / MTok
Claude Opus 4.6	$5 / MTok	        $6.25 / MTok	$10 / MTok	    $0.50 / MTok	        $25 / MTok
Claude Opus 4.5	$5 / MTok	        $6.25 / MTok	$10 / MTok	    $0.50 / MTok	        $25 / MTo

гипотеза 4: операторы не разговаривают много, они много делают, значит на токены это дёшево.

будем юзать клод соннет 5, он норм.

```deepseek
представь что ты -- оператор техподдержки крупного онлайн-сервиса. придумай 5 вопросов: 3 нетривиальных, 2 тривиальных. затем напиши ответы на них и переведи приблизительно в токены claude
---
Тривиальный вопрос 1: «Я забыл пароль. Как мне его сбросить?»
≈ 210 токенов Claude

Тривиальный вопрос 2: «Как поменять адрес электронной почты, привязанный к аккаунту?»
≈ 250 токенов Claude

Нетривиальный вопрос 3: «У нас бизнес-аккаунт. Коллега случайно удалил файл из общей папки, и прошло больше 30 дней, поэтому корзина пуста. Можно ли восстановить файл из более старых резервных копий?»
≈ 490 токенов Claude

Нетривиальный вопрос 4: «Я работал офлайн в десктопном приложении, а после подключения к интернету синхронизация создала конфликтную копию. Как объединить изменения из неё с основным файлом без потери данных?»
≈ 450 токенов Claude

Нетривиальный вопрос 5: «Можно ли дать доступ к подпапке только определённой группе, если родительская папка расшарена на весь отдел? Как правильно настроить иерархию прав?»
≈ 480 токенов Claude
```

mean(тривиальные): 400 (плюс амортизация)
mean(нетривиальные): 1000 (плюс амортизация)
mean(all): 700

Гипотеза 5: ср арифм тривиальных вопросов по токенам -- 400. ср арифм нетривиальных -- 1000. вместе -- 700. 

гипотеза 6: Инпутные токены дешевле аутпутных в 5 раз, и запросы часто будут короче чем ответы. следовательно, можно взять цену инпутных как 1/10 оутпутных.

Гипотеза 7: RAG -- от 100 до 2000 токенов к запросу.

Гипотеза 8: захерачим классификатор. на вход будет принимать эмбеддинги текста, подавать их в какую-нибудь нейронку и выдавать степень опасности/риска/критичности. сверхоператор будет задавать допустимую степень риска, и в зависимости от неё будут маршрутизироваться запросы. такой штуки в опенсорсе скорее всего нет, так что вот где будем использовать архитектуру.

Гипотеза 9: расчёты для стоимости.
берём тарифы клода на 09.08.

цена цикла вопрос/ответ в клоде
RAG: (2000/1e6)*2.5=0.05$
mean_cost_triv=0.05+(400/1e6)*(25+2.5)=0.061$=4.88р
mean_cost_nontriv=0.05+(1000/1e6)*(25+2.5)=0.0875$=7р

Гипотеза 10: CSAT и RR это дрисня которую я не смогу пруфануть так как она от меня не зависит.

арх 1: создаем БОГА. бог анализирует нагрузку и сам выставляет допустимость риска. если нагрузка высокая -- риск повышается. 

гипотеза 11: если обращений много -- что-то отъебнуло. оператор должен ввести в системный промпт сообщение об ошибке, чтобы робот его транслировал.

арх 2: клод посоветовал на типовые просто выдавать готовый ответ, у меня тоже крутилось на языке. можно лучше -- типовой ответ -> ллм -> живой оператор 

арх 3: у нас на сервере будет 2 штуки: штука-классификатор и штука-искатор

```claude
Один нюанс, который стоит явно проговорить в ml.md: у тебя получаются два независимых embedding-lookup (risk-classifier и FAQ-matcher), но вектор входящего тикета считается один раз и переиспользуется для обоих — это экономит вызовы модели эмбеддингов и стоит упомянуть как оптимизацию hot path.
```

```claude
Что стоит явно написать в ml.md: rubert-tiny2 — общий языковой эмбеддер, не дообучен под домен поддержки, поэтому граница между T/N может быть нечёткой на реальных данных; в продакшене это место для fine-tune или contrastive learning на исторических тикетах.
```

риск 1: всё посыплется если клод загнется а нейропузырь схлопнется, т.е. если нейронки станут сверхдорогими/не станут вовсе.
контрмера -- обучить свою нейронку) но это в высшей степени нецелесообразно.

ФИКС: вес tiny руберта -- 100mb.

арх 4: пока что экспериментальный THRESH -- 0.7. из эксперимента 

бля как круто
